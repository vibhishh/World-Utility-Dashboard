apiVersion: v1
kind: Service
metadata:
  name: world-utility
  labels:
    app: world-utility
spec:
  selector:
    app: world-utility
  type: NodePort
  ports:
    - port: 80
      targetPort: 9090
      nodePort: 30008
